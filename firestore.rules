rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
    function signedIn() {
      return request.auth.uid == "palyov.dimitar@gmail.com" ;
    }
      allow write;
    }
    match /{document=**} {
      allow read;
    }
  }
}